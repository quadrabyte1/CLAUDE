# Local SQLite workspace DB — each machine keeps its own
db/workspace.db
db/workspace.db-shm
db/workspace.db-wal

# MovieScanner — local scanner DB + downloaded IMDB dataset dumps
MovieScanner/db/scanner.db
MovieScanner/db/scanner.db-shm
MovieScanner/db/scanner.db-wal
MovieScanner/data/*.tsv.gz

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
.pytest_cache/

# Env files — may contain SMTP or other credentials
.env
app/.env

# macOS
.DS_Store

# Editor / OS
*.swp
.idea/
.vscode/
